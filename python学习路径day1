def factor (num):
    count = num // 2
    while count > 1:
        if num % count == 0:
            print('%d的最大约束是%d'% (num,count))
            break
        count -= 1
    else:
        print(num,'是素数')
num = int(input('输入一个数'))
factor(num)

        
